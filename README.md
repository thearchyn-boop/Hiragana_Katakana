Trainer for Hiragana and Katakana. Generates symbols and requires an input. Once the input is sent, user gets confirmation if the input was correct or not.

Currently uses spaced repetition to focus more on the problematic symbols. 
